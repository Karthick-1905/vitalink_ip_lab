# Test Results

This directory stores generated test logs and markdown summaries from:

```bash
./scripts/collect_test_evidence.sh
```

The intent is to produce a simple artifact you can show as execution evidence:

- raw frontend test log
- raw backend test log
- a one-page markdown summary with pass/fail status and log locations
